// 全局类型定义
// 统一从 shared 模块导出，桌面端和 WebUI 共用

export type {
  Protocol,
  TaskState,
  FileInfo,
  TaskStatus,
  TaskInfo,
  TaskParams,
  TaskUpdateEvent,
  DownloadHistory,
  GlobalStat,
  SpeedDataPoint,
  AppConfig,
} from "../shared/types";
